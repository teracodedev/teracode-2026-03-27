{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && [ -n \"$(git status --porcelain)\" ] && git commit -m 'update' && git push",
            "timeout": 60,
            "statusMessage": "git commit & push 中..."
          }
        ]
      }
    ]
  }
}
